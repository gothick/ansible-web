<IfModule mod_ssl.c>
    # SSL session cache for better performance
    SSLSessionCache shmcb:/var/cache/apache2/ssl_scm(512000)
    SSLSessionTickets On

    # OCSP Stapling
    SSLUseStapling On
    SSLStaplingCache shmcb:/var/cache/apache2/ssl_stapling(128000)
</IfModule>
