all:
  children:
    dev:
      hosts:
        vagrant:
          ansible_ssh_host: 192.168.56.2
          ansible_ssh_private_key_file: ~/.ssh/ansible
          ansible_user: ansible
          is_vagrant: true
